<resources>
    <!-- Base application theme. -->
    <style name="Theme.ComposeInputField" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
    </style>

    <style name="Theme.ComposeInputField.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ComposeInputField.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ComposeInputField.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>